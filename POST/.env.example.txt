# Пример переменных окружения (для вспомогательных скриптов, если появятся).
# Power Query в Excel читает путь к CSV из именованной области в книге — см. README.md.

# Пример (не используется запросами PQ по умолчанию):
# DATA_DIR=/путь/к/папке/с/Excel/
